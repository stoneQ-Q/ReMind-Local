# ReMind 云端改造进度清单

> 建立日期：2026-07-29  
> 当前阶段：腾讯云香港隔离测试环境
> 详细方案：[CLOUD_ARCHITECTURE_V1.md](./CLOUD_ARCHITECTURE_V1.md)

这份文档是云端改造的持续进度记录。每完成一项，都必须同时：

1. 完成对应验证；
2. 将任务标记为 `[x]`；
3. 在“完成记录”中写明日期、结果和 Git 提交号；
4. 确认没有破坏下方列出的安全底线。

## 一、已经确定的方案

- [x] 保留“自助部署版”：沿用当前 Mac 本地方案，用户自行配置 API，只支持手机和 Mac 在同一网络。
- [x] 建设“云端托管版”：用户安装 App 后直接使用，不依赖电脑。
- [x] 云端版提供三种 AI 模式：关闭 AI、使用用户自己的 API Key、使用 ReMind 托管额度。
- [x] 用户自己的 API Key 只通过加密连接提交，由服务端加密保存，不写入日志或 App 普通数据库。
- [x] 托管 AI 使用预付费余额：先预占、后结算、失败释放，禁止负余额。
- [x] 视频等高成本任务必须显示预计费用，并设置单任务、单用户和全平台成本上限。
- [x] 私密测试首选腾讯云香港；先完成本地云端化，再购买服务器。
- [x] 腾讯云不是代码依赖：使用 Docker、标准 PostgreSQL、对象存储和密钥服务适配层，保留迁移到阿里云或大陆节点的能力。
- [x] Cloudflare 只作为当前开发环境或海外辅助，不作为国内正式版的唯一核心服务。

## 二、安全底线

以下内容在整个改造过程中都必须保留：

- `remind.db` 及其现有数据；
- App SecureStore 中的微信和设备凭据；
- Cloudflare D1 中的现有数据；
- 当前微信登录、配对和网关配置；
- Obsidian 授权及同步配置；
- 当前 1.0.2 的本地可用链路。

禁止把以下内容提交到 Git、写入日志或放进客户端安装包：

- 用户或平台的 API Key；
- 微信令牌、连接密钥和会话凭据；
- 数据库密码和云服务密钥；
- 可直接访问用户私有文件的临时地址。

## 三、执行清单

### 阶段 0：冻结当前基线

- [x] 阅读项目交接文档并确认当前运行方式。
- [x] 验证 App 类型检查和 Android 构建。
- [x] 验证服务端和微信网关测试。
- [x] 检查敏感文件和 Git 忽略规则。
- [x] 建立当前 1.0.2 的首个可回退 Git 提交。
- [x] 编写云端架构与成本熔断方案。

### 阶段 1：统一接口和数据边界

- [x] 定义本地版与云端版共用的服务能力接口。
- [x] 定义用户、设备、微信连接、笔记、任务、文件、密钥和账本的所属关系。
- [x] 建立云端 API 的版本边界，避免 App 直接依赖某一家云服务。
- [x] 定义本地模式与云端模式的配置切换方式。
- [x] 为接口和数据边界补充自动化测试。
- [x] 验证现有本地链路不受影响。

### 阶段 2：本机运行云端基础设施

- [x] 使用容器启动 PostgreSQL。
- [x] 建立多用户数据库迁移。
- [x] 建立 API 服务、任务队列和 Worker 的最小运行骨架。
- [x] 实现账号、设备注册和登录会话。
- [x] 实现会话到期前自动续期，正常使用不要求重复输入恢复码。
- [x] 验证两个测试用户之间的数据完全隔离。
- [x] 建立自动备份与恢复演练。

### 阶段 3：API Key 与 AI 模式

- [x] 实现“关闭 AI”模式。
- [x] 实现用户自带 API Key 的加密提交和托管。
- [x] 确认 API Key 不进入日志、错误信息和普通数据库字段。
- [x] 实现 ReMind 托管 AI 模式状态；真实调用等待计费阶段。
- [x] 实现密钥轮换、删除和供应商切换。
- [ ] 正式收费前接入云 KMS。

### 阶段 4：计费与成本保护

- [x] 建立只追加、不直接修改历史记录的余额账本。
- [x] 实现费用预估、额度预占、实际结算和失败释放。
- [x] 实现单任务、单用户每日/月度和全平台预算上限。
- [x] 实现高费用任务二次确认。
- [x] 实现第三方异常自动暂停与告警。
- [x] 用故障测试证明不会出现重复扣款或负余额。

### 阶段 5：异步云端处理

- [x] 将微信轮询迁移为按用户隔离的 Worker 任务。
- [x] 将网页和小红书解析迁移到 Worker。
- [x] 将图片、视频和语音识别迁移到 Worker。
- [x] 实现任务重试、幂等、取消和超时。
- [x] 实现对象存储临时文件的自动清理。
- [x] 验证失败任务不会丢失原始记录。

### 阶段 6：App 云端功能

- [x] 实现账号登录和设备管理。
- [x] 实现本地模式/云端模式选择。
- [x] 实现 AI 模式和 API Key 设置。
- [x] 实现余额、费用明细和充值入口。
- [x] 实现高费用确认和异步任务状态。
- [x] 验证升级安装不会清空现有本地数据。
  - [x] 固定应用标识、数据库名、schema 版本和各类本地存储键。
  - [x] 使用隔离 SQLite 文件与模拟 SecureStore 完成自动化升级演练。
  - [x] 使用原 1.0.2 安装直接覆盖 build 4 APK，并由用户确认数据全部保留。

### 阶段 7：腾讯云香港私密测试

- [x] 根据实际本机资源测试结果选择服务器规格。
- [x] 配置腾讯云香港测试环境、对象存储和异地备份。
  - [x] 购买并初始化香港测试服务器。
  - [x] 部署隔离的 PostgreSQL、API、Worker 和本机备份运行时。
  - [x] 实现腾讯 COS 私有对象存储适配器和安全冒烟工具。
  - [x] 创建并接入同地域腾讯 COS 私有存储桶。
  - [x] 配置异地加密备份并完成恢复演练。
- [x] 完成 HTTPS、密钥、日志脱敏、监控和告警。
- [x] 给 App 私密测试构建写入云端 API 地址，并保留原本地模式。
- [ ] 连续进行 7 至 14 天移动、联通、电信网络测试。
- [ ] 测试微信、链接抓取、视频下载和模型接口出口稳定性。
- [ ] 根据实测成本确定体验额度与正式价格。
- [ ] 所有功能达到验收条件后再邀请私密测试用户。

## 四、完成记录

### 2026-07-29

- 完成阶段 0。
- App 类型检查、Android 导出、服务端 25 项测试、微信网关 10 项测试均通过。
- 建立 ReMind 1.0.2 安全基线，提交：`b696218`。
- 写入云端架构、AI 模式、计费熔断和迁移路线，提交：`91f1a73`。
- 确认本机私密配置未进入 Git，现有微信、数据库和 Obsidian 状态未改动。
- 完成阶段 1 的服务契约与数据边界：
  - 自助部署默认继续使用原 `/api` 路由；
  - 云端显式使用 `/api/v1`；
  - 旧版 SecureStore 凭据作用域保持不变；
  - 明确全部云端私有资源必须归属于唯一 `user_id`。
- 新增兼容性测试，并与现有测试合并验证，共 12 个测试文件、38 项测试通过。
- Android 导出成功，本地 API、Worker 和微信网关均在线。
- 开始阶段 2：
  - Docker Desktop 已安装并正常运行；
  - PostgreSQL 17.10 已启动并通过健康检查，仅监听 `127.0.0.1:5432`；
  - 首版多用户 schema 已实际执行，覆盖账号、设备、微信连接、笔记、任务、文件、加密凭据和只追加账本；
  - 跨用户账本关联、负余额和账本修改均被数据库拒绝，事务测试后数据已回滚为零；
  - 最小 API 和 Worker 已通过容器构建并运行，API 仅监听 `127.0.0.1:8790`；
  - API `/health` 与 `/ready` 均返回成功；
  - Worker 测试任务只执行一次、保持用户归属、费用为零，测试数据已清理。
- 完成阶段 2：
  - 私密测试账号方式确定为“自动创建匿名账号 + 恢复码”；
  - 注册时同时创建用户、零余额账户、设备和 30 天会话；
  - 恢复码、设备密钥和会话令牌均只保存哈希；
  - 使用恢复码可以给原账号增加新设备，错误恢复码会被拒绝；
  - 两个 API 测试账号只能读取自己的资料和设备；
  - 测试完成后用户、设备、会话、恢复凭据和余额账户均清理为零；
  - 迁移服务会校验已应用 SQL 的哈希，全新空数据库已从 0001、0002 成功建立 13 张表；
  - 每日自动备份已运行，保留期为 7 天；
  - 真实备份已恢复到临时数据库并验证 13 张表、2 个迁移，临时数据库已删除。
- 阶段 2 实现提交：`a28461a`。
- 阶段 2 补充完成无感续期：
  - 会话有效期仍为 30 天，App 在剩余 7 天或更少时自动续期；
  - 续期使用 SecureStore 中的设备密钥，不使用恢复码；
  - 同一时刻多个请求只执行一次续期；
  - 新会话生效后旧会话立即失效；
  - 容器冒烟测试已验证续期、新旧令牌状态、账号恢复和双用户隔离。
- 无感续期实现提交：`7ce2131`。
- 开始阶段 3：
  - 默认模式为“关闭 AI”，没有凭据时不能切换到用户自带 Key；
  - 支持 DeepSeek 与智谱凭据独立配置、替换、删除和切换；
  - 用户 Key 使用 AES-256-GCM 加密，绑定用户、供应商和密钥版本；
  - API 只返回末四位，完整 Key 未出现在响应或容器日志；
  - 删除最后一个用户 Key 后自动回到“关闭 AI”；
  - 托管 AI 当前仅保存模式，在预付费账本完成前不会发起真实调用；
  - 支持新旧主密钥共存和批量重加密，轮换冒烟测试通过；
  - 测试账号、API Key 和凭据记录均已清理为零；
  - 云 KMS 尚未接入，因此阶段 3 仍保持进行中。
- AI 模式与加密凭据实现提交：`3e7df13`。
- 开始阶段 4：
  - 金额统一使用人民币微元整数，避免浮点数产生舍入误差；
  - 账本记录每次余额与预占额度的变化量及操作后快照，历史记录继续由数据库禁止修改和删除；
  - 托管任务必须先预占，成功后按实际费用结算并释放差额，失败或取消则释放全部预占；
  - 入账、预占、结算和释放均使用幂等键；相同请求并发执行也只改变一次余额；
  - 已建立单任务、单用户每日/月度、全平台每日/月度五级硬上限；
  - App 当前只能查询自己的余额和费用明细，充值接口尚未开放；
  - 独立临时数据库故障测试覆盖重复请求、并发请求、余额不足、各级超限、账本篡改和负余额，测试库运行后自动删除；
  - 主开发库已无损应用第 3 个迁移，备份恢复验证通过 14 张表和 3 个迁移；
  - 全项目 19 个测试文件、73 项测试通过，云端 6 个测试文件、32 项测试通过，Android 导出成功；
  - 测试完成后主库用户、API Key 和账本记录均为零，现有本地数据、D1、微信与 Obsidian 配置未改动。
- 预付费账本与成本保护实现提交：`acb069f`。
- 完成阶段 4：
  - 高于或等于 1 元的任务必须使用服务端报价，并在 15 分钟内由用户明确二次确认；
  - 客户端不能提交预计费用，预占层还会独立拒绝未确认的高费用任务和被篡改的报价金额；
  - 报价和确认严格按 `user_id` 隔离，重复确认与重复预占使用固定幂等键；
  - DeepSeek、智谱在 5 分钟内连续失败 3 次会自动暂停、写入内部严重告警并拒绝新报价和新预占；
  - 自动暂停后不会因一次成功探测自行恢复，只能通过未公开的内部运维方法恢复；
  - 第 4 个数据库迁移已无损应用到主开发库；
  - 独立临时数据库实测通过报价确认、过期、跨用户隔离、报价防篡改、连续失败熔断、单次告警和人工恢复；
  - 备份恢复验证通过 16 张表和 4 个迁移，临时测试库与恢复库均已自动删除；
  - 云端类型检查、构建及 7 个测试文件 34 项测试通过；
  - 全项目 20 个测试文件 75 项测试通过，Android 导出成功；
  - 主库用户、API Key、账本与内部告警记录均为零；
  - PostgreSQL、云端 API、云端 Worker、原本地 API、原本地 Worker 和微信网关均正常。
- 高费用确认与供应商熔断实现提交：`d369572`。
- 开始阶段 5：
  - 建立按用户公平调度的通用异步任务执行器，单个用户的积压任务不会长期挤占其他用户；
  - 多 Worker 使用数据库行锁、唯一租约和执行令牌，只有当前租约持有者能够提交结果；
  - 每次尝试单独保留执行历史，失败按指数退避重试，达到上限后才进入最终失败；
  - 排队中与运行中的任务均可取消，任务状态和取消 API 严格按 `user_id` 隔离；
  - 任务有独立超时上限，即使处理器不响应取消信号，执行器也会停止等待；
  - Worker 崩溃后会回收过期租约并安全重试，旧 Worker 的迟到结果会被拒绝；
  - 最终失败或取消会释放已有费用预占，原始 `input_json` 在重试、超时、取消和崩溃恢复后保持不变；
  - 第 5 个数据库迁移已应用，真实 Worker 的 `system.noop` 任务只执行一次且执行历史完整；
  - 独立临时数据库验证了用户公平性、并发单次领取、失败重试、硬超时、排队/运行中取消、租约恢复、跨用户隔离和费用释放；
  - 备份恢复验证通过 18 张表和 5 个迁移；
  - 云端 8 个测试文件 37 项测试、全项目 21 个测试文件 78 项测试通过，Android 导出成功；
  - 主库用户、任务、执行历史、调度记录和账本均为零，原本地 API、Worker 与微信网关保持正常。
- 通用异步任务执行器实现提交：`97325b7`。
- 阶段 5 微信云端轮询完成：
  - 审计并保持了现有 Mac 微信网关的登录、长轮询、发送者过滤、消息去重、文字/语音归一化和回复模式契约；
  - 云端每个用户最多保留一个有效微信连接，凭据使用 AES-256-GCM 加密并绑定用户 ID 与连接 ID；
  - `wechat.poll` 任务输入只保存连接 ID 和连接代次，不保存令牌、微信用户 ID 或其他完整凭据；
  - 微信协议出站仅允许 HTTPS 的 `weixin.qq.com` 官方域名及其子域；
  - 轮询消息按用户和外部消息 ID 幂等入库，非绑定发送者会被过滤，文字和语音转写分别保留类型；
  - 失败不会推进游标，并由通用执行器退避重试；回复使用稳定客户端 ID，重新绑定会取消旧任务并使旧连接代次失效；
  - 独立临时数据库使用两个模拟用户、假凭据和模拟微信协议，验证了密文存储、无密钥任务输入、跨用户隔离、重复消息、错误发送者、回复幂等、断线恢复和安全重新绑定；
  - 第 6 个数据库迁移已无损应用，主开发库现有 19 张表和 6 个迁移；用户、微信连接、微信消息和任务均为零；
  - 备份恢复验证通过 19 张表和 6 个迁移；
  - 云端 11 个测试文件 45 项测试、全项目 24 个测试文件 86 项测试通过，Android 导出成功；
  - 云端 API 与 Worker、原本地 API、原本地 Worker 和微信网关均正常，未读取或迁移现有微信配置、`remind.db`、D1 或 Obsidian 授权。
- 微信云端轮询实现提交：`d19cc37`。
- 阶段 5 网页与小红书云端解析完成：
  - 审计并保持了现有服务端网页正文提取、网关小红书图文/视频识别和链接上下文契约；
  - 微信捕获中的公开链接会保存为用户自己的链接笔记，并自动进入 `link.parse` 队列；
  - 任务输入只保存笔记 ID 和处理代次，不复制原始网址或页面内容；
  - 每次请求和重定向都会拒绝账号密码 URL、非常规端口、本机/局域网/云元数据地址及私有或保留 IP；
  - DNS 解析后的全部地址必须为公网地址，实际连接固定到已校验地址，降低 DNS 重绑定风险；
  - 抓取限制为最多 4 次重定向、单次 15 秒、任务总计 45 秒、压缩前后 2 MB 和正文 24,000 字；
  - 普通网页保存标题、说明、站点和正文；小红书图文只保存经过 HTTPS CDN 域名校验的图片地址；
  - 小红书视频只保存类型和可用时长，临时视频下载地址不会写入笔记、任务输入或任务结果；
  - 独立临时数据库使用两个模拟用户和模拟网页响应，验证了用户隔离、微信链接入队、失败重试、取消回收、同步版本、网页/小红书元数据和临时地址排除；
  - 第 7 个数据库迁移已无损应用，主开发库保持 19 张表；迁移数为 7，用户、笔记和任务均为零；
  - 备份恢复验证通过 19 张表和 7 个迁移；
  - 云端 14 个测试文件 57 项测试、全项目 27 个测试文件 98 项测试通过，Android 导出成功；
  - 云端 API 与 Worker、原本地 API、原本地 Worker 和微信网关均正常，模拟测试未访问真实小红书或产生第三方费用。
- 网页与小红书云端解析实现提交：`f4f74aa`。
- 阶段 5 对象存储与临时文件自动清理完成：
  - 建立云厂商无关的对象存储接口；本地开发实现文件系统适配器，并预留腾讯 COS 与阿里 OSS 供应商边界；
  - 媒体二进制保存在独立 `remind-object-data` Docker 卷，PostgreSQL 只保存用户归属、对象键、类型、大小、SHA-256、用途和生命周期状态；
  - 对象键由服务端按 `users/<user_id>/<purpose>/<随机文件ID>` 生成，用户文件名不会成为路径；
  - 图片、音频和视频分别限制为 20 MB、100 MB 和 1 GB，并使用允许列表限制文件类型；
  - 本地写入使用私有目录、`0600` 权限和原子落盘，读取时重新校验租户、大小与 SHA-256；
  - 临时文件默认保留 24 小时，可配置范围为 5 分钟至 7 天；源文件和结果文件不受临时清理规则影响；
  - 清理使用数据库租约和逐次历史，支持多 Worker 并发、删除失败退避、租约过期接管、对象已不存在和进程崩溃恢复；
  - 超过 10 分钟仍为 `pending` 的半上传文件会自动回收，删除失败期间保持不可读取；
  - 独立临时数据库和临时目录验证了跨用户隔离、数据库不含二进制、并发到期清理、源文件保留、失败重试、租约恢复、半上传回收和删除历史；
  - 第 8 个数据库迁移已无损应用，主开发库现有 20 张表、8 个迁移；用户、文件和删除历史均为零；
  - PostgreSQL 备份恢复验证通过 20 张表和 8 个迁移；已明确数据库备份不包含对象二进制，正式环境必须单独配置 COS 生命周期和备份；
  - 云端 17 个测试文件 63 项测试、全项目 30 个测试文件 104 项测试通过，Android 导出成功；
  - 云端对象卷可写，云端 API 与 Worker、原本地 API、原本地 Worker 和微信网关均正常。
- 对象存储与自动清理实现提交：`b49cea5`。
- 阶段 5 AI 媒体任务执行与计费安全层完成：
  - `ai.text`、`ai.image`、`ai.video` 和 `ai.transcription` 任务不会仅因进入队列就被执行；
  - BYOK 任务必须由服务端明确标记为已授权且费用为零，托管任务必须先完成正数费用预占；
  - 有费用预占的处理器必须返回实际成本，缺少成本或实际成本超过预占都会使任务失败并释放全部预占；
  - 成功结果、实际扣费、未使用预占释放和执行历史在同一个 PostgreSQL 事务内提交，任一步失败都会整体回滚；
  - 无法序列化保存的任务结果在开始结算前即被拒绝，故障测试确认不会留下扣款或费用预占；
  - 独立临时数据库验证了未确认任务拦截、未预占任务拦截、BYOK 零费用执行、托管任务成功结算、缺成本、成本超限和结果保存失败；
  - 云端 17 个测试文件 63 项测试、全项目 30 个测试文件 104 项测试通过，Android 导出成功；
  - 本地云端镜像已重建，API、数据库和 Worker 正常；主库用户、任务、账本和文件均为零，备份恢复仍为 20 张表和 8 个迁移。
- AI 任务执行与原子结算实现提交：`6e1d8fc`。
- 阶段 5 媒体 Worker 模拟工作流完成：
  - 建立按用户归属的持久化媒体请求，支持幂等创建、状态查询、取消和失败状态保留；
  - 图片使用 `ai.image` 单步分析，音频使用 `ai.transcription` 转写；
  - 视频拆为 `media.video.prepare`、`ai.transcription`、`ai.text` 三个独立任务，任务输入不保存源文件 ID、对象键或二进制；
  - 视频准备阶段只生成短期临时音轨，转写完成后立即标记到期并由对象清理 Worker 删除，原始图片、音频和视频保持不变；
  - 独立临时数据库验证了跨用户隔离、幂等冲突、音频失败重试、视频分步推进、排队取消、临时音轨清理、源文件保留和零费用账本；
  - 模拟供应商默认关闭，只有显式配置 `REMIND_MEDIA_PROVIDER=mock` 才会注册，未知配置会拒绝启动，避免未来部署误把模拟结果当作真实分析；
  - 第 9 个迁移已无损应用到主开发库，现有 21 张表、9 个迁移；用户、任务、媒体请求、账本和文件均为零；
  - 备份恢复验证通过 21 张表和 9 个迁移；
  - 云端 17 个测试文件 63 项测试、全项目 30 个测试文件 104 项测试通过，Android 导出成功；
  - 云端 API、数据库和 Worker、原本地 API、原本地 Worker 和微信网关均正常，模拟测试没有网络调用或第三方费用。
- 媒体 Worker 模拟工作流实现提交：`5c42870`。
- 阶段 5 真实媒体供应商安全边界完成：
  - 审计确认现有图片观察与语音识别使用智谱，视频使用服务端 FFmpeg 分片、智谱/本地 Whisper 转写和 DeepSeek 整理；Mac 本地 Whisper 继续只属于自助部署版；
  - 建立智谱图片、智谱 ASR 和 DeepSeek 视频总结的独立 HTTPS 客户端，固定供应商地址并禁止 HTTP 重定向；
  - API Key 只进入 `Authorization` 请求头，不进入请求正文、任务数据或供应商错误信息；
  - 上游响应限制为 2 MB，并校验媒体大小、响应 JSON、输出长度、DeepSeek JSON 结构及 prompt/completion token 用量；
  - 建立 BYOK 与托管凭据路由：BYOK 只解密当前用户对应供应商的 Key，托管只读取平台 Key 且要求任务已有正数费用预占；
  - 零费用 AI 任务现在必须在领取瞬间确认用户仍为 BYOK 模式；关闭 AI 或切换托管后不会继续执行旧的零费用任务；
  - 托管媒体价格表改为服务端完整配置，图片、ASR、DeepSeek 输入和输出价格缺少任一项就拒绝报价，计算全程使用整数微元并向上取整；
  - 自动化测试覆盖固定地址、密钥隔离、响应上限、无敏感错误、BYOK 缺 Key、托管缺预占、价格缺失和整数计价；
  - 云端 21 个测试文件 81 项测试、全项目 34 个测试文件 122 项测试通过，Android 导出成功；
  - 本机云端镜像已重建，API、数据库和 Worker 正常；媒体真实供应商仍未注册，主库用户、任务、媒体请求、账本和文件均为零；
  - 备份恢复继续通过 21 张表和 9 个迁移。
- 真实媒体供应商安全边界实现提交：`3ab954f`。
- 阶段 5 BYOK 媒体真实执行链路完成：
  - 新增 BYOK 媒体请求创建、状态查询和取消 API，只接受当前用户状态为 `ready` 的源文件；
  - 图片和音频在入队前要求智谱 Key，视频同时要求智谱与 DeepSeek Key；缺失凭据、关闭 AI、跨用户文件和幂等冲突均被拒绝；
  - Worker 每次调用前重新核对用户仍为 BYOK 并在内存中解密对应 Key，切换模式或删除 Key 后旧任务不会继续调用；
  - BYOK 用户自己的 4xx 凭据错误不会计入全平台供应商熔断，429、5xx、网络或非法响应仍会进入健康统计；
  - 视频源文件先物化到 Worker 私有临时目录，并以大小和 SHA-256 重新校验；FFmpeg 只读取本地文件，不接收远程 URL、Cookie 或鉴权头；
  - 视频按 28 秒切片，单片最大 5 MB、最多 800 片；每片保存为用户自己的临时对象，逐片转写后带时间戳合并并统一到期清理；
  - 新增媒体片段归属表，第 10 个迁移已无损应用，主开发库现有 22 张表、10 个迁移；
  - Docker 镜像拆为通用运行层和 Worker 层，只有 Worker 安装 FFmpeg，API 与迁移镜像不再承担额外体积；
  - 临时数据库验证了 BYOK 凭据要求、真实路由选择、两段视频转写、时间戳合并、片段清理、重试、取消和源文件保留；网络响应继续使用模拟实现；
  - 云端 23 个测试文件 87 项测试、全项目 36 个测试文件 128 项测试通过，Android 导出成功；
  - Worker 内 FFmpeg 8.1.2 可用，API 镜像确认不含 FFmpeg；API、数据库、云端 Worker、原本地 API、原本地 Worker 和微信网关均正常；
  - 主库用户、任务、媒体请求、媒体片段、账本和文件均为零，备份恢复通过 22 张表和 10 个迁移。
- BYOK 媒体真实执行链路实现提交：`d07876e`。
- 阶段 5 托管媒体统一费用信封完成：
  - 图片、音频和视频托管请求先生成整条流水线的服务端总报价，并保持在“待确认”状态；用户确认并完成一次预占后才会进入处理队列；
  - 视频准备、分片转写和总结使用内部任务类型，内部任务不生成报价、不单独预占，也不会要求逐片确认；
  - 每个阶段只上报服务端验证的模拟实际成本并累计到媒体请求，累计值不得超过整条流水线的预占上限；
  - 成功时只对父级费用信封统一结算一次并释放差额；任一阶段失败或取消时释放尚未结算的全部预占，恢复流程可安全重复执行；
  - Worker 领取托管内部任务前会重新验证用户仍为托管模式、父级信封仍为已预占状态且金额与请求一致，伪造的内部任务不能绕过费用授权；
  - 新增 `media.pipeline` 父级报价类型和媒体请求的执行模式、父级任务、预计成本、累计实际成本及结算恢复状态；
  - 第 11 个迁移已无损应用，主开发库保持 22 张表、迁移数为 11；用户、任务、媒体请求、媒体片段、账本和文件均为零；
  - 独立临时数据库验证了图片和三阶段视频都只确认一次、内部任务零预占、成功统一结算、取消完整释放和账本幂等；未使用真实凭据、未访问供应商、未产生费用；
  - 云端 23 个测试文件 87 项测试、全项目 36 个测试文件 128 项测试通过，Android 导出成功；
  - PostgreSQL 备份恢复通过 22 张表和 11 个迁移；云端 API、数据库、Worker、原本地 API、原本地 Worker 和微信网关均正常，真实托管媒体供应商和收费入口继续保持关闭。
- 托管媒体统一费用信封实现提交：`a1c0222`。
- 阶段 5 托管媒体真实供应商连接层完成：
  - 新增显式 `remote` 媒体模式，同时支持 BYOK 与托管执行；默认仍为 `disabled`，`mock` 仍只允许测试，未知模式拒绝启动；
  - 平台智谱与 DeepSeek Key 只注入 Worker，不注入 API、App、任务输入或数据库；`remote` Worker 缺任一平台 Key 或价格项都会拒绝启动；
  - 托管图片按服务端图片单价计量，智谱 ASR 按 Worker 使用 `ffprobe` 核验后的音视频时长计量，DeepSeek 按响应中的 prompt/completion token 用量计量；
  - 视频总报价按 28 秒分片逐片向上取整，并为最大转写文本预留总结 token 上限，避免分片舍入或总结用量突破费用信封；
  - 用户申报的音视频时长只用于报价上限；真实执行前由 Worker 从私有本地文件核验，实际时长超过报价时会在调用供应商前失败并释放预占；
  - `POST /api/v1/media/requests` 在 `remote` 模式下会按用户 AI 模式分别创建 BYOK 请求或托管总报价；托管请求继续复用现有一次确认与预占 API；
  - 托管平台 Key 的 401/403 与通用 429、5xx、网络/非法响应会计入全平台熔断；连续三次失败自动暂停供应商并只写入一条开放告警，BYOK 用户自身的普通 4xx 不受影响；
  - 临时 API 测试进程未配置任何平台 Key，验证了托管报价创建、一次确认预占、请求查询、缺失时长拒绝和取消释放；真实供应商客户端使用模拟响应验证平台 Key 路由及图片、ASR、token 实际成本换算；
  - 云端 25 个测试文件 93 项测试、全项目 38 个测试文件 134 项测试通过，Android 导出成功；未访问真实供应商、未产生费用；
  - 本机云端镜像已重建，媒体模式保持 `disabled`；主开发库保持 22 张表、11 个迁移，用户、任务、媒体请求、媒体片段、账本和文件均为零；
  - PostgreSQL 备份恢复通过 22 张表和 11 个迁移；云端 API、数据库、Worker、原本地 API、原本地 Worker 和微信网关均正常。
- 托管媒体真实供应商连接实现提交：`c183ff9`。
- 阶段 5 手机媒体安全上传完成：
  - 新增创建、查询、分片续传、完成和取消上传 API，手机无需一次性把大文件完整保存在请求内；
  - 上传会话默认保留 24 小时，单片最大 4 MB；服务端用已确认偏移量串行接收分片，网络中断后可从服务端偏移量继续；
  - 文件在完成前始终保持 `pending` 且不可读取，只有总大小、SHA-256 和 JPEG、PNG、WebP、MP3、MP4、MOV、WAV、Ogg、WebM 文件签名全部通过后才会原子发布；
  - 创建和完成操作支持幂等，所有上传、文件与查询均按用户隔离；同一偏移量的并发请求只会写入一次；
  - 对象已经发布但数据库事务中断时可以安全恢复；取消、过期和清理失败均有租约、退避与重试，不会把半文件暴露给媒体 Worker；
  - 对象存储接口同时定义了本地代理分片与云厂商直传分片两种策略，已预留腾讯 COS／阿里 OSS 的创建分片、签名分片、查询已上传分片、完成和中止边界；
  - 第 12 个迁移已无损应用，主开发库现有 23 张表、12 个迁移；用户、任务、上传会话、媒体请求、账本和文件均为零；
  - 独立临时数据库与真实 HTTP API 验证了幂等、跨用户隔离、断点续传、并发串行、大小与 SHA 校验、文件签名、崩溃恢复、取消、过期和清理失败重试；
  - 云端 25 个测试文件 97 项测试、全项目 38 个测试文件 138 项测试通过，Android 导出成功；
  - PostgreSQL 备份恢复通过 23 张表和 12 个迁移；云端 API、数据库、Worker、原本地 API、原本地 Worker 和微信网关均正常；
  - 媒体供应商模式继续保持 `disabled`，未访问真实供应商、未产生费用，现有 `remind.db`、SecureStore、D1、微信配置和 Obsidian 授权未改动。
- 手机媒体安全上传实现提交：`1d35aaf`。
- 阶段 6 App 账号登录与设备管理完成：
  - App 首页新增“云”入口，未配置私密测试云端时会明确显示仍在使用本地模式，不影响记录、搜索和本地备份；
  - 用户可以直接开通匿名云端账号，不需要邮箱或手机号；已有用户可以使用恢复码在新手机恢复同一个账号；
  - 恢复码只在首次开通后显示一次，用户确认安全保存前不能关闭页面；写入 SecureStore 的对象只包含会话凭据，不再包含一次性恢复码；
  - App 会展示账号下的全部有效设备、当前设备标记和最近使用时间，支持移除旧设备或退出当前设备；
  - 新增服务端设备撤销 API，撤销设备和它的所有有效会话在同一个事务内完成，旧访问令牌立即失效；
  - 设备查询与撤销严格按用户隔离，其他账号对设备 ID 发起操作只会得到不存在响应；
  - 当前设备退出只清除对应云端会话，不删除 App 本地数据库、笔记或 Obsidian 配置；重新连接需要恢复码；
  - 网络超时、恢复码错误和会话失效均转换为可理解的中文提示，并明确说明本地笔记不受影响；
  - 云端 25 个测试文件 97 项测试、全项目 38 个测试文件 139 项测试通过，iOS 与 Android 导出成功；
  - 真实 HTTP 冒烟测试通过注册、30 天会话续期、账号恢复、设备列表、跨用户隔离、移除旧设备和退出当前设备；
  - 本机云端镜像已重建，API 与 PostgreSQL 健康、Worker 正常且媒体供应商保持 `disabled`；主库 23 张表、12 个迁移，用户、设备、会话、任务、上传、媒体请求、账本和文件均为零；
  - PostgreSQL 备份恢复继续通过 23 张表和 12 个迁移，原本地 API、Worker 与微信网关保持正常。
- App 云端账号与设备管理实现提交：`fb640d5`。
- 阶段 6 本地模式／云端模式选择完成：
  - App“连接方式”页面同时展示本地模式与云端模式，并明确说明 Mac 与同一局域网限制、数据位置、API 成本归属、外网可用性和当前收费状态；
  - 新增独立的 `EXPO_PUBLIC_REMIND_LOCAL_API_URL` 与 `EXPO_PUBLIC_REMIND_CLOUD_API_URL` 配置，两套服务地址、路由规则和 SecureStore 凭据作用域互不覆盖；
  - 继续兼容旧版 `EXPO_PUBLIC_REMIND_API_URL`，现有本地开发配置和微信设备凭据不会因升级失效；
  - 用户选择保存在系统安全存储；App 启动时只有目标模式仍然存在才会恢复，否则安全回退到可用模式；
  - 切换前先探测本地 `/health` 或云端 `/ready`，只有目标服务健康且模式选择成功写入安全存储后才改变运行中路由；
  - 目标未配置、连接超时、健康检查失败或安全存储写入失败时保持原模式，并显示可理解的中文提示；
  - 切换只影响之后的服务请求，不修改 `remind.db`，不自动上传、删除或迁移本地笔记，也不清除另一模式的账号和设备凭据；
  - 云端模式当前明确标注只开放账号与设备，托管 AI 和收费尚未开启；本地专用的微信与整理接口不会被错误发送到云端 V1 API；
  - 从云端切回本地后会恢复本地微信连接，并重新检查尚待整理的本地链接；
  - 新增模式恢复、独立地址与凭据作用域、健康探测成功、失败不切换和旧配置兼容测试；
  - 全项目 39 个测试文件 144 项测试、云端 25 个测试文件 97 项测试通过，iOS 与 Android 导出成功；
  - 本地 `/health` 与云端 `/ready` 均通过，云端 API 与 PostgreSQL 健康、Worker 正常且媒体供应商保持 `disabled`；原本地 API、Worker 和微信网关保持正常；
  - 本步骤没有数据库迁移，主库继续保持 23 张表、12 个迁移且业务表为空。
- App 本地与云端模式选择实现提交：`254d143`。
- 阶段 6 App AI 模式与 API Key 设置完成：
  - 云端账号页面新增“AI 与 API Key”入口，提供“不使用 AI”“使用自己的 API Key”“使用 ReMind 托管服务”三种清晰选择；
  - 托管服务继续显示为“尚未开放”且不能选择，余额、价格展示和充值入口完成前不会从 App 启用；
  - 用户可以分别保存、替换和删除 DeepSeek 与智谱 Key，并明确说明两者分别承担文字／视频总结和图片／语音／视频转写；
  - 保存第一个自己的 Key 后自动切换到 BYOK；关闭 AI 不删除已有密文，删除最后一个 Key 时服务端自动回到关闭 AI；
  - 完整 Key 只存在于当前输入框和一次云端 API 请求中，提交成功或关闭页面后立即清空，不写入 SecureStore、`remind.db` 或日志；
  - 服务端继续按用户和供应商使用 AES-256-GCM 关联加密，数据库只额外保存末四位；App 查询结果只接受供应商、末四位和更新时间；
  - 已保存的完整 Key 不提供显示或下载；界面提示怀疑泄露时应在供应商后台撤销并生成新 Key；
  - AI 设置请求固定使用独立云端 V1 地址和当前云端账号会话，不会把 Key 发给本地模式地址或其他用户；
  - 会话失效会清除本机云端会话并引导用户使用恢复码重新连接；超时、Key 格式错误和缺少凭据均显示中文提示；
  - 新增 App 客户端测试，覆盖鉴权路由、Key 请求体、只接受脱敏响应、关闭模式、单独删除、稳定错误码和失效会话清理；
  - 真实 HTTP 冒烟测试通过 AI 设置读取、加密 Key 保存、末四位返回、跨用户隔离、模式切换、替换与删除；测试 Key 已清理；
  - 全项目 40 个测试文件 149 项测试、云端 25 个测试文件 97 项测试通过，iOS 与 Android 导出成功；
  - 本步骤没有数据库迁移，主库继续保持 23 张表、12 个迁移，用户、设备、会话、API Key、任务和账本均为零；备份恢复通过；
  - 云端媒体供应商与真实收费继续保持 `disabled`，本地 API、Worker、微信网关及云端 API、数据库、Worker 均保持正常。
- App AI 模式与 API Key 设置实现提交：`d0997fa`。
- 阶段 6 App 余额、费用明细与锁定充值入口完成：
  - 云端账号页面新增“余额与费用”入口，展示当前可用余额、账户总余额、任务预占、每日上限和每月上限；
  - App 全程使用服务端整数微元字符串和 `BigInt` 解析金额，不使用浮点数计算余额；显示时最多保留实际存在的六位小数；
  - 客户端会重新验证“可用余额 = 总余额 - 预占金额”以及预占不超过余额，响应无法对账时拒绝展示；
  - 费用明细展示最近 50 笔只追加账本记录，区分体验额度赠送、充值到账、任务预占、实际结算、预占释放、退款和人工调整；
  - 服务端只从账本内部元数据中返回 `payment`、`gift`、`operator` 三种允许的来源标签，不把支付引用、运维备注或其他内部元数据暴露给 App；
  - 每条明细同时展示发生后的余额和预占，任务记录会标明 AI 任务，用户可以下拉刷新；
  - 空账本会明确提示“使用自己的 API Key 不会从这里扣费”，避免用户把 BYOK 供应商账单误认为 ReMind 余额；
  - 充值入口显示“私密测试暂不充值”和“无付款入口”，点击只解释支付、退款、对账与合规尚未完成；
  - App 没有支付链接、金额输入、订单创建或收款 API，本步骤不会接受或记录任何真实付款；
  - 新增统一云端鉴权请求层，AI 设置与费用中心共用会话刷新、超时、稳定错误码和 401 会话清理；
  - 新增金额格式、余额对账、账本类型、未知来源拒绝和客户端响应上限测试；
  - 云端完整计费冒烟测试继续通过不可修改账本、幂等预占／结算／释放、硬性额度和非负余额，并验证只返回允许的充值来源标签；
  - 全项目 41 个测试文件 153 项测试、云端 25 个测试文件 97 项测试通过，iOS 与 Android 导出成功；
  - 本步骤没有数据库迁移，主库继续保持 23 张表、12 个迁移，用户、设备、会话、API Key、任务和账本均为零；备份恢复通过；
  - 云端镜像已重建，API 与 PostgreSQL 健康、Worker 正常且媒体供应商保持 `disabled`；原本地 API、Worker 与微信网关保持正常。
- App 余额与费用中心实现提交：`a14ce79`。
- 阶段 6 App 高费用确认与异步任务状态完成：
  - 云端账号页面新增“任务与确认”入口，统一展示当前账号最近 50 个图片、语音和视频任务；
  - 新增按用户隔离的任务列表 API，一次返回媒体请求、父级费用报价和当前执行任务；其他账号无法看到任务或报价；
  - App 展示待确认、排队中、处理中、自动重试次数、结算中、取消中、成功、失败和已取消状态，进行中任务每 4 秒自动刷新；
  - 预计费用、是否属于高费用、15 分钟有效期和预占金额全部来自服务端；App 不接收或计算用户提交的报价；
  - 高费用任务不会自动确认，只有用户再次看到明确金额并点击“确认并预占”后，服务端才会预占余额并允许 Worker 处理；
  - 报价过期后不能确认；余额不足、每日／每月上限和供应商自动暂停都会阻止任务开始并显示中文提示；
  - 用户可取消待确认、排队中或处理中的任务，未使用的预占会释放，已经实际产生的第三方成本仍按实际用量结算；
  - 客户端严格校验任务状态、整数微元、报价时间、重试次数和响应上限，异常响应不会被当作有效费用或任务状态展示；
  - 真实 HTTP 与临时 PostgreSQL 冒烟测试通过任务列表、跨用户隔离、服务端报价、一次确认预占、取消释放、重试、超时和熔断；
  - 全项目 42 个测试文件 157 项测试、云端 25 个测试文件 97 项测试通过，iOS 与 Android 导出成功；
  - 本步骤没有数据库迁移，真实托管供应商、充值和收费入口继续保持关闭，未访问真实供应商、未产生费用；
  - 现有 `remind.db`、SecureStore、D1、微信配置和 Obsidian 授权未改动。
- App 高费用确认与异步任务状态实现提交：`2a64e58`。
- 阶段 6 覆盖升级自动化保护完成，真机覆盖安装待验收：
  - 将 Android package、iOS bundle identifier、`remind.db`、SQLite schema v16、微信设备凭据、云端会话、服务模式和 Obsidian 设置键集中定义为稳定升级契约；
  - App 不再允许构建环境变量替换数据库名，避免新安装包因误配置打开另一份空数据库而表现为数据丢失；
  - 自动化测试固定 1.0.2 已使用的应用标识与全部存储键，未来任何无意改名都会直接导致测试失败；
  - 在临时目录建立真实 `remind.db`，写入笔记、微信导入记录、Obsidian 目录授权和已导出记录，关闭后由当前版本重新打开并执行迁移；
  - 升级前后逐字段快照完全一致，SQLite schema 仍为 v16，`integrity_check` 与外键检查通过；
  - 模拟升级前 SecureStore 验证当前代码仍能读取既有微信设备 ID／密钥、服务模式和云端 30 天会话；
  - Expo 配置继续包含 `expo-sqlite` 与 `expo-secure-store`，Android package 和 iOS bundle identifier 均保持 `app.remind.notes`；
  - 本机 D1 的设备、微信绑定、消息、连接器和迁移数量在验证前后保持一致，微信登录配置文件指纹未变化；
  - Worker、微信网关和本地 API 全程在线；原本地 Worker 25 项、微信网关 10 项、云端 97 项测试通过；
  - 全项目 43 个测试文件 161 项测试通过，iOS 与 Android 导出成功；
  - EAS 首次 build 3 因根依赖锁缺少两个间接依赖记录而在安装阶段停止，没有生成安装包；已使用与 EAS 相同的 npm 版本同步锁文件并通过严格安装检查，提交：`64d178b`；
  - Android preview build 4 已构建成功，EAS build ID：`f654f9db-7917-41a1-9c64-a3706bc5a37e`；
  - 新 APK 已下载到 `/Users/stone/Downloads/ReMind-1.0.2-build4-upgrade-test.apk`，SHA-256 为 `8d544fc8901fbe33de13433ea57e62d4bdd35a75294ed2d1f9bbc3e2620d15ee`；
  - 新旧 APK 都包含 `app.remind.notes` 与版本名 1.0.2，APK Signing Block 中的签名证书 SHA-256 完全一致，可以由 Android 作为原 App 的覆盖更新安装；
  - 当前机器没有 Android 模拟器或已连接测试设备，因此没有对用户现有手机执行有风险的覆盖安装，阶段 6 最后一项暂不提前标记完成。
- 覆盖升级自动化保护提交：`de1af17`。
- 阶段 6 真机覆盖升级验收完成：
  - 用户已在完成数据导出后，从现有 ReMind 1.0.2 直接覆盖安装 Android build 4，没有先卸载旧版；
  - Android 接受同 package、同签名证书和更高构建号的原位更新；
  - 用户确认升级后数据全部保留，自动化演练与真机结果一致；
  - Worker、微信网关和本地 API 在验收后继续正常运行；
  - 阶段 6“App 云端功能”至此全部完成，仍未开启真实托管供应商、充值或收费。
- 阶段 7 本机资源基准与首台服务器规格完成：
  - 云端 API、PostgreSQL、Worker 与备份容器空载合计约 107 MiB；
  - 完整功能链单次 2.69 秒，4 组并发 3.27 秒完成，PostgreSQL 观察峰值约 76 MiB；
  - 256 MB 文件以 64 个 4 MB 分片完成写入、SHA-256 校验和原子发布，用时 0.61 秒，测试进程最大 RSS 约 137 MiB；
  - 1 小时 1080p 视频按正式 FFmpeg 参数提取为 129 个音频片段，用时 4.55 秒，Worker 峰值约 66 MiB；4 个同类任务并发用时 7.44 秒、峰值约 187 MiB，确认主要瓶颈是 CPU；
  - 私密测试首台服务器确定为腾讯云香港 Linux 2 核 4 GB、70 GB SSD、30 Mbps、每月 2 TB 流量的入门型套餐，当前刊例价 90 元/月，首次只购买 1 个月；
  - 视频 Worker 初始并发固定为 1，用户媒体进入同地域 COS，系统盘保留至少 20 GB 可用空间；
  - 腾讯云明确提示香港入门型不保证中国内地跨境公网质量，因此购买后必须完成 7 至 14 天移动、联通、电信测试，未达标时评估优选流量包或 CVM；
  - 基准期间真实媒体供应商继续保持关闭，使用隔离数据库与对象目录，未产生模型费用；生成的测试媒体已清理。

### 2026-07-30

- 阶段 7 腾讯云香港隔离测试环境完成：
  - 已购买并初始化腾讯云轻量应用服务器，地域为中国香港一区；
  - 实机验证为 Ubuntu Server 22.04 LTS、2 核 CPU、约 3.6 GiB 内存、69 GiB 系统盘；
  - 已完成系统安全更新、仅密钥 SSH、禁止 root／密码登录、Fail2ban、UFW 和 Docker 官方运行环境；
  - 云端运行时使用 Git 提交 `7fb5b44`，PostgreSQL、API、Worker 和每日备份容器均已启动；
  - API 健康检查成功，PostgreSQL 共 23 张表、12 个迁移，用户和账本数据均为零；
  - 数据库 `5432` 与 API `8790` 只监听服务器本机，外部探测关闭；HTTP／HTTPS 当前也未开放；
  - 媒体供应商保持 `disabled`，平台 Key、价格、收费入口和真实微信连接均未启用，没有产生模型费用；
  - 首份数据库备份已生成，并成功恢复到临时数据库，核对通过 23 张表和 12 个迁移；
  - 自动备份脚本的默认文件权限已收紧，重新发布后新生成备份实测为 `0600`；但当前仍在同一台服务器上，因此异地备份尚未完成；
  - 现有本地 `remind.db`、SecureStore、D1、微信配置和 Obsidian 授权均未读取、迁移或改动；
  - 详细部署边界与后续顺序见 `CLOUD_PRIVATE_TEST_DEPLOYMENT_2026-07-30.md`。
- 阶段 7 腾讯 COS 私有对象存储适配器完成：
  - 使用腾讯云官方 Node.js COS SDK，实现 `local`／`tencent_cos` 显式提供商切换；默认仍为 `local`；
  - 手机继续使用现有 4 MB 分片续传，服务端先在私有目录校验大小、SHA-256 和文件签名，再通过 HTTPS 写入同地域 COS；
  - COS 对象写入时强制使用 SSE-COS AES-256，并保存 SHA-256 私有元数据；写入完成、读取和物化时都会核对大小、加密状态或内容完整性；
  - 对象键继续由服务端生成并保持用户前缀隔离，App 不接触长期 COS 密钥、公开地址或签名；
  - COS 请求错误会转换为有限的稳定错误，不返回请求 URL、SecretId、SecretKey 或供应商原始错误正文；
  - 新增真实 COS 冒烟工具，启用后会完成加密写入、读取、下载和删除，并自动清理测试对象；
  - 最小 CAM 权限只需要指定桶 `users/*` 下的 Put、Get、Head 和 Delete，不需要列桶、修改 ACL、删除桶或其他云产品权限；
  - 中国香港标准存储当前刊例约 0.136 元/GB/月；同地域服务器与 COS 默认走免费内网流量，但请求仍计费；
  - 云端类型检查、25 个测试文件 100 项测试、生产 Compose 校验和 Docker 镜像构建通过，生产依赖审计为 0 个已知漏洞；
  - 实现提交 `2a422d5` 已部署到香港服务器，API、PostgreSQL 和 Worker 健康，当前对象提供商实测仍为 `local`；
  - 云端数据库保持 23 张表、12 个迁移，用户和文件均为零；尚未创建 COS 桶、CAM 子账号或密钥，没有产生 COS 请求和费用；
  - 存储桶选择、费用、最小权限与回滚步骤见 `TENCENT_COS_PRIVATE_TEST_SETUP.md`。
- 阶段 7 同地域腾讯 COS 私有存储桶接入完成：
  - 已创建中国香港 `ap-hongkong` 私有存储桶 `remind-private-hk-1385855631`，启用 SSE-COS，关闭版本控制、CDN、全球加速、静态网站和日志存储；
  - 已配置 7 天后删除未完成分块上传的生命周期规则，不删除正常业务文件；
  - 已创建仅编程访问的 CAM 子用户 `remind-cos-runtime`，只关联 `ReMindCOSRuntimeMinimalAccess`；
  - CAM 策略只允许指定桶 `users/*` 对象的 Put、Get、Head 和 Delete，不允许列桶、修改桶配置或访问其他云产品；
  - 凭据通过本地 CSV 直接写入服务器私密环境文件，未显示在聊天、Git 或日志中；本地 CSV 权限已收紧为 `0600`；
  - 独立 COS 冒烟测试通过加密写入、读取、下载完整性校验和删除，测试对象已清理；
  - API 与 Worker 已切换到 `tencent_cos` 并完成重建，API 健康检查、数据库就绪检查和启动日志均正常；
  - 切换前后用户、文件和上传记录均为 0，无需迁移已有对象；真实媒体供应商继续保持 `disabled`。
- 阶段 7 新加坡异地加密数据库备份完成：
  - 已创建新加坡 `ap-singapore` 私有备份桶 `remind-db-backup-sg-1385855631`，与香港业务服务器分离；
  - 备份桶启用版本控制与 SSE-COS，当前版本保留 30 天、历史版本保留 7 天、未完成分块上传保留 7 天；
  - 已创建独立 CAM 子用户 `remind-backup-runtime`，仅允许备份桶 `postgres/*` 的 Put、Get 和 Head，不允许 Delete、List 或修改桶配置；
  - 备份在服务器端使用独立密钥进行 AES-256-GCM 流式加密，再通过 SSE-COS 二次加密写入；
  - 加密密钥保存在服务器 `0600` 环境文件和当前 Mac 系统钥匙串，COS 凭据与运行时媒体凭据相互隔离；
  - 本地 27 个测试文件、104 项测试、TypeScript 检查、生产 Compose 校验和备份容器构建通过，生产依赖审计为 0 个已知漏洞；
  - 实现提交 `82591de` 已部署，自动备份容器每天生成本机备份并上传新加坡 COS；
  - 真实演练完成生成、AES-256-GCM 加密、跨地域上传、下载、密文 SHA-256、GCM 认证、解密和 `pg_restore`；
  - 恢复后的临时数据库核对为 23 张表、12 个迁移，随后已删除；临时恢复数据库、用户、文件和上传记录均为 0；
  - 已实际验证备份账号的删除请求被腾讯云拒绝，自动备份容器重启次数为 0；
  - 详细配置、密钥位置和灾难恢复顺序见 `TENCENT_COS_ENCRYPTED_BACKUP.md`。
- 阶段 7 临时域名和 HTTPS 公网入口完成：
  - 私密测试临时域名确定为 `remind.43-129-237-189.sslip.io`，A 记录已解析到香港服务器；
  - 加入固定版本 Caddy 网关、自动 HTTPS、安全响应头和认证接口限流，API 与数据库继续只监听服务器本机；
  - Caddy 只加入前端网络，PostgreSQL 只加入后端网络，只有 API 同时连接两者；
  - 本地类型检查与 28 个测试文件、106 项测试全部通过，服务器上的 Caddy 配置验证通过；
  - 实现提交 `0e1f84c` 已部署，API、PostgreSQL、Worker 和备份容器均健康；
  - 腾讯云公网防火墙已只新增 TCP `80` 和 `443`，Let’s Encrypt 可信证书签发成功；
  - HTTP 到 HTTPS 的 `308` 跳转、TLS 域名验证、健康接口、安全响应头和外部网络访问均已通过；
  - 公网 `8790` 和 `5432` 保持关闭，Caddy 无法访问数据库网络；
  - 注册接口限流实测第 6 次请求返回 `429`，运行日志未发现认证头、API Key 或云密钥标记；
  - 上线后数据库仍为 23 张表、12 个迁移，用户和任务均为零；
  - 临时域名只用于私密测试，正式对外前需要替换成自有域名。
- 阶段 7 本地健康巡检完成：
  - 新增只读 systemd 定时器，每 5 分钟检查 HTTPS、5 个长期容器、磁盘、内存和最近数据库备份；
  - 阈值为磁盘 80%、内存 90%、完整备份最长 30 小时，不读取私密环境文件；
  - 巡检只记录正常或异常状态，不自动重启数据库、删除数据或开启任何收费功能；
  - 正常路径和故意触发磁盘阈值的故障路径均已在真实服务器验证；
  - 实现提交 `3761c7f` 已部署，定时器开机启用，首次巡检成功；
  - 腾讯云资源告警、费用预算和短信／邮件通知仍待在控制台完成。
- 阶段 7 腾讯云资源告警完成：
  - `ReMind-HK-resource-alert` 已绑定香港实例，监控 CPU 85%、内存 85%、磁盘 80% 和外网带宽使用率 80%；
  - 四项指标均按一分钟粒度连续 5 个数据点判断，每 30 分钟最多重复告警一次；
  - `ReMind-HK-traffic-package-alert` 已绑定同一实例，网络流量包剩余量不高于 20% 时每天最多告警一次；
  - 两条策略均使用系统预设通知模板，主账号本人全天接收邮件和短信，并包含告警恢复通知；
  - 未配置自动扩容、自动重启或其他自动操作。
- 阶段 7 月度费用预算完成：
  - `ReMind-private-test-monthly` 已持续生效，覆盖腾讯云账号全部费用；
  - 月度预算为人民币 120 元，实际费用达到 80%（96 元）和 100%（120 元）时通知；
  - 创建时当月实际费用为 90 元，占预算 75%，提醒尚未触发；
  - 预算只负责提醒，不会自动授权新增产品，也不会自动阻止消费。
- 阶段 7 余额预警完成：
  - 当前可用余额为人民币 0 元，已订阅余额预警；
  - 阈值设为 0 元，只有后付费项目使余额低于 0 元时才通知；
  - 未开启自动充值，不要求为了测试环境长期保留较大账户余额。
- 阶段 7 App 私密测试地址配置完成：
  - Android `preview` 内部分发构建已写入 `https://remind.43-129-237-189.sslip.io`；
  - 原局域网本地服务地址继续保留，既有安装默认行为和本地数据位置不变，用户可在 App 内主动切换；
  - 云端地址属于公开构建配置，不包含服务器、COS、数据库或模型密钥；
  - `preview` 构建显式使用 EAS `preview` 环境，仍输出可直接安装的 APK；
  - 新增自动化测试，固定私密测试构建同时具备本地与 HTTPS 云端入口，并禁止生产配置复用临时 `sslip.io` 地址；
  - 本步骤仅完成配置和验证，不创建测试账号、不迁移本地数据，也不触发新的 EAS 构建。
- 阶段 7 Android 单人测试包构建完成：
  - EAS Android `preview` build 5 构建成功，Build ID 为 `4e83e00a-ba82-4f4e-b7aa-5ecee6c5c435`；
  - 构建固定对应 Git 提交 `738892c`、Expo SDK 54、应用版本 1.0.2 和 Android 构建号 5；
  - APK 已下载到 `/Users/stone/Downloads/ReMind-1.0.2-build5-private-cloud.apk`；
  - APK SHA-256 为 `a1e9f5fdb819eba6e8bbe8f326cbec60b74fe6c6df17118b56397d7c90f68b34`，ZIP 完整性检查通过；
  - APK 内确认包含 `app.remind.notes`、版本名 1.0.2、香港 HTTPS 云端地址和原局域网本地地址；
  - build 5 与已通过真机覆盖升级验收的 build 4 签名证书 SHA-256 均为 `7b7b023385569627d3465d3547b2afc9dcfd1579d2431a41fb1f5f1f06f7978d`；
  - 用户已直接覆盖安装 build 5，并确认原有本地数据全部正常保留；
  - 覆盖安装没有创建云端账号，也没有迁移本地数据到云端。
- 阶段 7 首个单人云端账号验证完成：
  - 用户已在 Android build 5 内切换云端模式并开通匿名云端账号；
  - 服务器只读核对为 1 个有效用户、1 台有效 Android 设备、1 个有效会话和 1 份有效恢复凭据；
  - 会话有效期为 30 天，AI 模式保持 `disabled`；
  - 云端笔记、文件、上传、任务、账本、API Key、微信连接和媒体请求均为 0，没有自动迁移本地内容；
  - 计费账户余额与预占金额均为 0，没有产生模型调用或费用记录；
  - 公网 `/ready` 返回数据库就绪，API、PostgreSQL、Worker、Caddy、备份容器和健康巡检保持正常；
  - 开通后最近 20 分钟 API 无错误、网关无 5xx，日志未发现认证头、恢复码、API Key 或云密钥标记。
- 阶段 7 首轮移动数据连接验证完成：
  - 用户完全关闭 Wi-Fi、仅使用手机移动数据重新打开 App；
  - App 仍显示“云端模式”和“账号已经开通”，云端会话无需重新登录或输入恢复码；
  - 中国联通和中国电信移动网络均已分别验证可正常连接；
  - 服务器侧确认有效会话刚刚刷新，API 最近 10 分钟无错误；
  - 云端笔记、任务和账本仍为 0，没有自动上传本地内容或产生费用；
  - 中国移动仍待单独验证，后续继续观察 7 至 14 天。
- 阶段 7 云端功能首轮只读验收与任务页修复进行中：
  - 用户已在移动网络下确认“AI 与 API Key”和“余额与费用”可以正常打开；
  - “任务与确认”在 build 5 中打开后导致 App 退出，服务器同期无 5xx，账号任务、媒体请求和账本均为空，问题收敛到 App 页面本身；
  - 任务页已移除不必要的请求锁和 AppState 轮询分支，改为与已验证正常的费用页一致的受控加载方式；
  - 新增任务页错误边界；即使以后某条异常任务数据触发渲染错误，也只显示安全返回页，不影响本地笔记、不提交任务、不产生费用；
  - TypeScript 检查、47 个测试文件共 172 项测试和 Expo Android 离线导出均已通过；
  - 修复提交为 `4b466ee`，EAS Android `preview` build 6 已成功完成，Build ID 为 `9cc949b8-abd8-4fad-ae9b-ea894c0efc70`；
  - build 6 继续使用版本名 1.0.2、包名 `app.remind.notes`、Android 构建号 6 和原 EAS 远端签名凭据；
  - APK 已下载到 `/Users/stone/Downloads/ReMind-1.0.2-build6-task-fix.apk`，SHA-256 为 `8c475ac23a99e5376f6cd66272b40cab7b3288753b6e4926606054841247a863`，ZIP 完整性检查通过；
  - 用户覆盖安装 build 6 后复测，“任务与确认”仍会导致 App 退出，说明页面内部请求锁、AppState 轮询和 React 渲染错误均不是充分根因；
  - USB 调试确认测试设备为荣耀 MBH-AN10、Android 16，已安装版本为 1.0.2 build 6，覆盖安装状态正常；
  - Android 系统保留的 build 5 和 build 6 崩溃记录均指向 React Native Fabric 原生挂载层：`RetryableMountingLayerException: Unable to find viewState`，调用点为 `SurfaceMountingManager.addViewAt`；
  - 根因是同一渲染批次关闭“连接方式”原生 Modal 并打开子页面 Modal，在该设备上触发旧视图删除与新视图创建竞态；因此 React 错误边界无法捕获；
  - 所有云端子页面切换已改为互斥状态，并在 Android 上等待 500ms，确保旧 Modal 完全销毁后再创建新 Modal；返回“连接方式”也使用相同保护；
  - 定点修复后 TypeScript 检查、47 个测试文件共 172 项测试和 Expo Android 离线导出再次通过；
  - build 7 已覆盖安装，首次安装时间保持不变；清空旧日志后实时复测仍出现同一 `Unable to find viewState` 原生崩溃，说明单纯分隔 Modal 事务仍不足以规避该设备的 Fabric 缺陷；
  - Expo SDK 54 官方仍支持在 App 配置中设置 `newArchEnabled: false` 使用旧渲染器；当前兼容测试版已关闭 New Architecture/Fabric，业务接口、数据库、包名和签名均不改变；
  - build 8 已生成并由用户覆盖安装，Android 构建号确认更新为 8，首次安装时间保持不变，说明本地数据仍按覆盖升级路径保留；
  - build 8 启动“连接方式”后没有立即出现此前的 Fabric 崩溃，但旧渲染器把该 Modal 内 ScrollView 的内容高度错误限制为可视区域高度，导致页面无法向下滚动；
  - 真机 UI 层级和自动滑动复现均确认滚动位置不变，问题属于页面布局测量，不涉及账号、笔记、SecureStore 或云端数据；
  - “连接方式”的 ScrollView 已改为只承载一个自然高度内容容器，避免旧渲染器对多个直接子节点的内容高度计算错误；
  - 修复后 TypeScript 检查和 47 个测试文件共 172 项测试通过；
  - 修复提交为 `fa65e5c`，EAS Android `preview` build 9 已成功完成，Build ID 为 `4162d044-d9da-4c58-892c-9cc1d1f05fec`；
  - build 9 继续使用版本名 1.0.2、包名 `app.remind.notes`、Android 构建号 9 和原 EAS 远端签名凭据；
  - APK 已下载到 `/Users/stone/Downloads/ReMind-1.0.2-build9-scroll-fix.apk`，SHA-256 为 `75c2944ca85dcbbd25f9502edc7a554521e3864682a590ad9318ff3bf2870c0b`，ZIP 完整性检查通过；
  - APK 已推送到手机 `/sdcard/Download/ReMind-1.0.2-build9-scroll-fix.apk`；
  - 用户覆盖安装 build 9 后，版本号确认为 9、首次安装时间保持不变，但真机从按钮区域和左右空白边缘多次滑动仍完全不产生滚动，说明单一内容容器不足以修复旧渲染器测量；
  - 用户回忆 build 7 的“连接方式”滚动正常；Git 精确对比确认 build 7 到 build 8 之间没有修改 `CloudAccountSettings`，唯一影响原生渲染的改动是 `app.json` 中关闭 New Architecture；
  - 因此滚动回归确定来自旧渲染器，不继续叠加 ScrollView 补丁；当前已恢复 build 7 的页面结构和 New Architecture；
  - 为避开 Fabric 崩溃，云端子页面改为覆盖在仍然存活的“连接方式”Modal 上：打开子页面时不再销毁账号 Modal，返回时也不再重新创建账号 Modal，从生命周期上消除旧视图删除与新视图创建竞态；
  - 新增构建保护测试，禁止后续配置再次显式关闭 New Architecture；TypeScript 检查、47 个测试文件共 172 项测试和 Expo Android 离线导出均已通过；
  - 修复提交为 `d20d4a6`，EAS Android `preview` build 10 已成功完成，Build ID 为 `3aecc6a0-3f85-4293-99fb-a4b667223232`；
  - build 10 继续使用版本名 1.0.2、包名 `app.remind.notes`、Android 构建号 10 和原 EAS 远端签名凭据；
  - APK 已下载到 `/Users/stone/Downloads/ReMind-1.0.2-build10-modal-fix.apk`，SHA-256 为 `2941708542f024af2b291269af032237102ae64136e562af3b579818262a2571`，ZIP 完整性检查通过；
  - APK 已推送到手机 `/sdcard/Download/ReMind-1.0.2-build10-modal-fix.apk`；
  - 用户覆盖安装 build 10 后，版本号确认为 10、首次安装时间保持不变；真机自动滑动确认“连接方式”已恢复正常，余额、任务、设备和最底部安全说明均可访问；
  - build 10 打开“任务与确认”仍触发同一 Fabric `Unable to find viewState` 崩溃，说明即使账号 Modal 保持挂载，创建第二个任务 Modal 本身仍会在该设备触发原生缺陷；
  - 任务中心已改为账号 Modal 内的绝对定位覆盖层，不再创建任何第二个原生 Modal；账号页面继续在下方保持挂载，打开时只增加同一 Fabric Surface 内的覆盖内容，关闭时只移除覆盖内容；
  - TypeScript 检查、47 个测试文件共 172 项测试和 Expo Android 离线导出均已通过；
  - 修复提交为 `1490dbc`，EAS Android `preview` build 11 已成功完成，Build ID 为 `1233a87c-0304-434b-85dd-5579b8233b2d`；
  - build 11 继续使用版本名 1.0.2、包名 `app.remind.notes`、Android 构建号 11 和原 EAS 远端签名凭据；
  - APK 已下载到 `/Users/stone/Downloads/ReMind-1.0.2-build11-embedded-tasks.apk`，SHA-256 为 `7dd01a89b7628bc02cc18d98b973043ee710f5933f83961f1ef8d0aedcd78cf7`，ZIP 完整性检查通过；
  - APK 已推送到手机 `/sdcard/Download/ReMind-1.0.2-build11-embedded-tasks.apk`；
  - 下一步由用户覆盖安装 build 11，复测任务中心同 Surface 方案。

## 五、下一项

阶段 7 的服务器基础环境、隔离云端运行时、腾讯 COS 私有对象存储、新加坡异地加密数据库备份、临时 HTTPS 公网入口、本地健康巡检、腾讯云资源告警、月度费用预算、余额预警、App 私密测试地址配置、首个云端账号、联通和电信移动数据连接，以及 AI 设置和费用中心只读访问已经完成。build 10 已确认恢复“连接方式”滚动，但第二个任务 Modal 仍触发 Fabric 缺陷；任务中心现已改为账号 Modal 同一 Surface 内的覆盖层，完全取消第二个原生 Modal。build 11 已完成并推送到手机，下一步覆盖安装后复测。
